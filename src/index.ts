import { merge } from "./utils/merge/index";
import { convertToTailwindcss } from "./utils/convert-to-tailwindcss";
import { repeat } from "./utils/repeat";

const tailwindcssUtils = {
  repeat,
  merge,
  convertToTailwindcss,
};

export default tailwindcssUtils;
